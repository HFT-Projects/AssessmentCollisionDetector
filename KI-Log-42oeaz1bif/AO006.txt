Claude 3.7 Sonnet:


1.Prompt: // Implement a filter for the max distance text field:
          // When the user enters a number (e.g. 50), only show assessments
          // where the collision distance is less than or equal to that number.
          // Additionally, if the distance value is 0 (or missing), it should still be shown
          // as long as the entered max distance is greater than or equal to 0.
          // Do not remove or rewrite existing logic unnecessarily.
          // Keep it simple and focused only on this filtering behavior.".


2.Prompt: // Modify the existing min-distance filter logic to behave as a max-distance filter.
          // Only show collisions where the distance is less than or equal to the entered value.
          // Additionally, if the distance is 0, it should always be shown regardless of the input,
          // as long as the user-specified max distance is >= 0.
          // Keep the existing structure of the filter; just change the condition logic.

3.Prompt:/ Implement two separate sorting dropdowns: one for Exam 1 and one for Exam 2.
         // Each dropdown allows selection of a column to sort by, and an additional control (e.g. a second dropdown or toggle button) to choose the sort direction (ascending or descending).

         // Exam 1 sort options:
         // - Exam 1 Name
         // - Collision Count
         // - Start Time
         // - End Time
         // - Average Distance
         // - Min Distance

         // Exam 2 sort options:
         // - Exam 2 Name
         // - Collision Count
         // - Start Time
         // - End Time
         // - Distance

         // Requirements:
         // - When the user selects a sorting criterion and direction (ascending or descending),
         //   sort the corresponding TreeTableView accordingly.
         // - Ensure that the sorting applies only to the selected exam (Exam 1 or Exam 2).
         // - Visually indicate the active sort column and direction, e.g. using arrow indicators in the column header.
         // - Default sort direction should be ascending unless specified otherwise.


4.Prompt: Recalculate assessment summary data after applying collision filters:
         //
         // For each visible Assessment in the table:
         // → Filter the list of visible Collisions (after filtering) that belong to that Assessment.
         //
         // Then recalculate:
         // 1. Collision Count: number of visible collisions assigned to this assessment.
         // 2. Min Distance: the smallest distance among the visible collisions.
         // 3. Average Distance: the mean distance among the visible collisions.
         //

5.Prompt:// In the DataInputPane, add a new section below "Import Files":
         // - Label: "Optional Year:"
         // - TextField: optionalYearField
         // - Button: applyYearButton

         // Place the label, text field, and button inside an HBox or VBox.