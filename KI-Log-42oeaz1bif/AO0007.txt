Claude 3.5:

1.Prompt: Create a new tab named "Optimized Exams", placed immediately after the "Collision Results" tab. // This tab must reuse the entire UI and logic from the "Collision Results" tab: // - Same TreeTableView setup // - Same filtering, sorting, and checkboxes // - All events and bindings should behave identically
          // Modify the table columns as follows: // 1. Keep existing columns: Exam 1, Exam 2, Collision Count // 2. Rename timing-related columns to "Old Begin", "Old End", "Old Distance", "Old Average Distance", "Old Min Distance" // 3. Next to each "Old" column, add a new one with: // - "Optim Begin" // - "Optim End" // - "Optim Distance" // - "Optim Average Distance" // - "Optim Min Distance"
          // The optimized columns can be left empty for now; placeholder data is sufficient. // Optimized assessments data will be connected later.

2.Prompt:       // Extend the "Optimized Exams" tab by duplicating the lower section from "Collision Results": // Include the following UI elements below the filter/sort section:
                // 1. A checkbox row with the following options (same functionality as in "Collision Results"): // - Hide Entries with No Times // - Show Only Assessments // - Show Only with Collisions
                // 2. A column visibility dropdown menu (reused from "Collision Results"). // Columns should include: // - Exam 1 (always visible and not hideable) // - Exam 2 (always visible and not hideable) // - Collision Count // - Old Begin, Old End, Old Distance, Old Average Distance, Old Min Distance // - Optim Begin, Optim End, Optim Distance, Optim Average Distance, Optim Min Distance
                // 3. Behavior for the dropdown column visibility menu: // - The five "Optim" columns must be shown by default // - The five corresponding "Old" columns must be hidden by default // - If the user wants to see the "Old" columns, they can toggle them manually in the dropdown
                // Reuse existing filtering and sorting logic from "Collision Results" // Just point it to the new OptimizedAssessments data structure once it's available // (For now, use placeholder values or an empty list to test layout and logic)
                // Ensure layout consistency with "Collision Results" (same spacing, padding, component structure)